######## PASSWORD LESS KEYS ###########

We Have three linux hosts ubuntu, centos and debian.

username = "lab" (all hosts)

ubuntu  = 192.168.1.10
centos  = 192.168.1.11
debian  = 192.168.1.12

#mapping of hostname to ip add

sudo vi /etc/hosts

192.168.1.11	centos
192.168.1.12	debian

#Check ping 

##Create SSH key on ansible control node (ubuntu) and accept the defaults

ssh-keygen -t rsa -b 4096

#list the keys to verify and copy the key to other machines

ls .ssh
ssh-copy-id -i .ssh/id_rsa.pub debian
ssh-copy-id -i .ssh/id_rsa.pub centos

#note if refused, run:
sudo apt install openssh-server # replace apt with yum in centos7

#Now configure debian so that it doesn't require a password 
#to get sudo level access

ssh debian
su -
sudo visudo

#go to bottom of file add this line:

lab ALL=(ALL) NOPASSWD: ALL

#Now configure centos so that it doesnt require a password 
#to get root level access

ssh centos
su -
sudo visudo

#go to bottom of file add this line:
lab ALL=(ALL) NOPASSWD: ALL
